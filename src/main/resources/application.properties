spring.application.name=workflowx

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/workflowx}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Dhiraj@123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

jwt.secret=${JWT_SECRET:WorkflowXSuperSecretKeyForJWTAuthentication2026SecureKey}